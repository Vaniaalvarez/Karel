class program {
    program () {
        // TODO poner codigo aqui
        while (notNextToABeeper)
        if(frontIsClear)
         {
          putbeeper();
          if(frontIsClear)
          { 
            move();
          }
         }
          else 
          {
            turnleft();
            turnleft();
            turnleft();
          }
        turnoff();
    }
}